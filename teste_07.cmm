int a; 
int b; 
int c;
int ma;
int me;

void main() {  
    a = b = c = 10;

    ++b;
    c--;

    me = a<=b && a<=c ? a : b <= c ? b : c; 
    ma = a>=b && a>=c ? a : b >= c ? b : c;   

    write (" a =  ", a );
    write (" b =  ", b );
    write (" c =  ", c );
    write (" me =  ", me );
    write (" ma =  ", ma );

}

